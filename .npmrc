# 使用官方 npm registry
registry=https://registry.npmjs.org/

# Scoped package 发布配置
@plain-ui:registry=https://registry.npmjs.org/

# 确保发布公开包（scoped packages 默认是私有的）
access=public

# 严格对等依赖
strict-peer-dependencies=false

# 自动安装对等依赖
auto-install-peers=true
